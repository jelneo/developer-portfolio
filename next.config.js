// @ts-check

/**
 * @type {import('next').NextConfig}
 **/
const nextConfig = {
  output: 'export',
  basePath: '/developer-portfolio',
  experimental: {
    appDir: true,
  },
}

module.exports = nextConfig
